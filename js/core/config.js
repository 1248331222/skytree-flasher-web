// flash_tool/static/js/config.js
// ============ 配置（WebUSB 独立项目） ============
// 项目已分离为纯 WebUSB 模式，不再需要后端地址配置。
// 保留空配置文件以维持 module-system.js 加载顺序兼容性。

(function() {
    // WebUSB 模式不使用后端地址
    App.backendUrl = '';
})();
